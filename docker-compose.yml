services:
  postgres:
    image: postgres:16-alpine
    container_name: sortlearn-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: sortlearn
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - '5433:5432'
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U postgres -d sortlearn']
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres-data:
